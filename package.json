{
  "name": "11tystarter",
  "version": "1.0.0",
  "description": "11tystarter",
  "main": "index.html",
  "scripts": {
    "watch:sass": "sass --no-source-map --watch src/scss:public/css",
    "watch:eleventy": "eleventy --serve",
    "build:sass": "sass --no-source-map src/scss:public/css",
    "build:eleventy": "eleventy",
    "start": "npm-run-all --parallel watch:*",
    "build": "npm-run-all build:sass build:eleventy",
    "clean": "rimraf ./public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/robjwood/rjw-11ty-starter.git"
  },
  "author": "Rob",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "npm-run-all": "^4.1.5",
    "sass": "^1.66.1"
  }
}
