{
  "name": "euro2024sweepstake",
  "version": "1.0.0",
  "description": "Euro 2024 sweepstake",
  "main": "index.html",
  "scripts": {
    "watch:sass": "sass --no-source-map --watch src/scss:public/css",
    "watch:eleventy": "eleventy --serve",
    "build:sass": "sass --no-source-map src/scss:public/css",
    "build:eleventy": "eleventy",
    "start": "npm-run-all --parallel watch:*",
    "build": "npm-run-all build:sass build:eleventy",
    "clean": "rimraf ./public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/robjwood/euro-2024-sweepstake.git"
  },
  "author": "Rob",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "npm-run-all": "^4.1.5",
    "sass": "^1.66.1"
  },
  "dependencies": {
    "@11ty/eleventy-fetch": "^4.0.1",
    "dotenv": "^16.4.5"
  }
}
